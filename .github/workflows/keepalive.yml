name: keepalive

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  keepalive:
    uses: feRpicoral/keepalive/.github/workflows/keepalive.yml@v1
    with:
      stale-days: 50
